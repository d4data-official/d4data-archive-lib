import { Following } from 'types/schemas/Following'
import Standardizer from 'classes/standardizer/Standardizer';

export default async function getFollowings(this: Standardizer): Promise<Following[]> {
  return [];
}
